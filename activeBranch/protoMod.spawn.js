if(!Spawn.prototype.AddCreep2Queue)
{
    /**
     * Adds provided creep to the Spawns' queue. 
     * @param {Creep} creep The creep to add to spawn queue
     * @returns True on creep added to queue, false otherwise
     */
    Spawn.prototype.AddCreep2Queue = function(creep)
    {
        var additionSuccess = true;
        return additionSuccess
    }
}